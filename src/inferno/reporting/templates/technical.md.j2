# Technical Findings Report

**Target:** {{ target }}
**Assessment Date:** {{ date }}
**Report Generated:** {{ generated_at }}

---

## Table of Contents

1. [Assessment Methodology](#methodology)
2. [Findings Summary](#summary)
3. [Detailed Findings](#findings)
4. [Remediation Priorities](#remediation)
5. [Appendix](#appendix)

---

## <a name="methodology"></a>1. Assessment Methodology

### Tools Used
{% for tool in tools_used %}
- {{ tool }}
{% endfor %}

### Phases Completed
{% for phase in phases %}
- [x] {{ phase }}
{% endfor %}

### Assessment Duration
- **Start Time:** {{ start_time }}
- **End Time:** {{ end_time }}
- **Duration:** {{ duration }}

---

## <a name="summary"></a>2. Findings Summary

### By Severity

| Severity | Count | Percentage |
|----------|-------|------------|
| Critical | {{ findings_by_severity.critical | default(0) }} | {{ (findings_by_severity.critical | default(0) / total_findings * 100) | round(1) if total_findings > 0 else 0 }}% |
| High | {{ findings_by_severity.high | default(0) }} | {{ (findings_by_severity.high | default(0) / total_findings * 100) | round(1) if total_findings > 0 else 0 }}% |
| Medium | {{ findings_by_severity.medium | default(0) }} | {{ (findings_by_severity.medium | default(0) / total_findings * 100) | round(1) if total_findings > 0 else 0 }}% |
| Low | {{ findings_by_severity.low | default(0) }} | {{ (findings_by_severity.low | default(0) / total_findings * 100) | round(1) if total_findings > 0 else 0 }}% |
| Info | {{ findings_by_severity.info | default(0) }} | {{ (findings_by_severity.info | default(0) / total_findings * 100) | round(1) if total_findings > 0 else 0 }}% |

### By Category

{% for category, count in findings_by_category.items() %}
- **{{ category }}:** {{ count }}
{% endfor %}

---

## <a name="findings"></a>3. Detailed Findings

{% for finding in findings %}
### {{ finding.id | default(loop.index) }}. {{ finding.name }}

| Attribute | Value |
|-----------|-------|
| **Severity** | {{ finding.severity | upper }} |
| **Category** | {{ finding.category | default('General') }} |
| **Location** | {{ finding.location | default('N/A') }} |
| **CVE** | {{ finding.cve | default('N/A') }} |
| **CVSS** | {{ finding.cvss | default('N/A') }} |

#### Description

{{ finding.description }}

#### Technical Details

{{ finding.technical_details | default('No additional technical details provided.') }}

{% if finding.evidence %}
#### Evidence

```
{{ finding.evidence }}
```
{% endif %}

{% if finding.steps_to_reproduce %}
#### Steps to Reproduce

{% for step in finding.steps_to_reproduce %}
{{ loop.index }}. {{ step }}
{% endfor %}
{% endif %}

#### Impact

{{ finding.impact | default('Security risk requiring attention.') }}

#### Remediation

{{ finding.remediation | default('Apply appropriate security controls.') }}

#### References

{% for ref in finding.references | default([]) %}
- {{ ref }}
{% endfor %}

---

{% endfor %}

## <a name="remediation"></a>4. Remediation Priorities

### Immediate (Critical/High)

{% for finding in findings if finding.severity in ['critical', 'high'] %}
1. **{{ finding.name }}** - {{ finding.remediation | default('See finding details') | truncate(100) }}
{% endfor %}

### Short-term (Medium)

{% for finding in findings if finding.severity == 'medium' %}
1. **{{ finding.name }}** - {{ finding.remediation | default('See finding details') | truncate(100) }}
{% endfor %}

### Long-term (Low/Info)

{% for finding in findings if finding.severity in ['low', 'info'] %}
1. **{{ finding.name }}** - {{ finding.remediation | default('See finding details') | truncate(100) }}
{% endfor %}

---

## <a name="appendix"></a>5. Appendix

### A. Raw Scan Data

Available in the artifacts directory: `{{ artifacts_dir }}`

### B. Tools Configuration

{% for tool, config in tool_configs.items() | default([]) %}
**{{ tool }}:**
```
{{ config }}
```
{% endfor %}

### C. Assessment Metrics

- **Total Requests:** {{ metrics.total_requests | default('N/A') }}
- **Total Tokens Used:** {{ metrics.total_tokens | default('N/A') }}
- **Assessment Turns:** {{ metrics.turns | default('N/A') }}

---

*Report generated by Inferno Autonomous Penetration Testing Agent*
